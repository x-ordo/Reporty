# Database (Neon)
DATABASE_URL="postgresql://user:pass@host/db?sslmode=require"

# Minimal admin auth (dev only)
ADMIN_API_KEY="change-me"

# Crypto
MASTER_KEY_B64="base64-32-bytes"
# Key versioning (optional now, required later)
MASTER_KEY_ID="v1"
# KEYRING_JSON='{"v1":"<base64-32bytes>","v2":"<base64-32bytes>"}'
PUBLIC_CODE_SECRET="long-random-string"
EVIDENCE_HMAC_KEY="base64-or-hex-random"

# R2 (S3 compatible)
R2_ENDPOINT="https://<accountid>.r2.cloudflarestorage.com"
R2_BUCKET_NAME="safereport"
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""

# QStash
QSTASH_TOKEN=""
PDF_LAMBDA_FUNCTION_URL="https://<lambda-function-url>"

# Optional seeding
SEED_PARTNER_NAME="Demo Labor Attorney Group"
SEED_TENANT_NAME="Demo SME (Tenant)"
# SEED_PARTNER_ID=""
# SEED_TENANT_ID=""
SEED_INVITE_CODE="IRONDEMO"

# Optional: Lambda -> callback to mark pack complete
# PDF_CALLBACK_TOKEN="change-me"

# (Lambda env) APP_CALLBACK_URL="https://<your-app>/api/internal/pdf-callback"
# (Lambda env) FONT_URL="https://<public-font-url>/NotoSansKR-Regular.otf"  # REQUIRED (fail fast if missing)

# Partner keys are stored hashed in DB (no env). Create via /admin/partners.
